projectVersion=2.0.0-SNAPSHOT
grailsVersion=7.0.8

# Comma-separated list of projects to publish
projectsToPublish=grails-phone-number-constraint

# Build dependencies
testLoggerVersion=4.0.0

# Enable build scans from GitHub workflows
ciBuildScanPublish=false

org.gradle.caching=true
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.jvmargs=-Dfile.encoding=UTF-8 -Xmx1024M
